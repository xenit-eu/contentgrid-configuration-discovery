plugins {
    id 'java-platform'
    id 'maven-publish'
}

dependencies {
    constraints {
        rootProject.subprojects.forEach {
            if (it != project) {
                api it
            }
        }
    }
}